from settings import *

def get_makeblastdb_command():
    # e.g.: makeblastdb -in infiles -title title - hash_index hash_index -out outname -max_file_sz settings_default -logfile settings_logfile
    pass

def get_blastp_command():
    # e.g.: blastp -db dbname -dbtype nucl/prot -evalue input_evalue -gapextend val -gapopen val -gilist fname -html fname? -out outname -outfmt formatnr
    pass
